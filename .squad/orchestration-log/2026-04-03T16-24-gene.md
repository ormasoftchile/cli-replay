# Orchestration Log — Gene (Core Dev)

**Timestamp:** 2026-04-03T16:24:00Z  
**Agent:** Gene — Gert Execution Engine Deep-Dive  
**Status:** ✅ Completed

## Summary

Performed deep read of gert's execution engine, tracing every CLI command invocation path through the codebase. Identified `providers.CommandExecutor` as the single critical integration point where cli-replay can hook all command execution. Evaluated three integration patterns ranked by implementation value and proposed RecordingExecutor wrapper pattern as immediate next step.

## Work Completed

- Traced gert execution engine from entry point through `gert exec` lifecycle
- Analyzed CommandExecutor interface and all three current implementations
- Mapped what flows through CommandExecutor (type: cli steps, type: tool stdio transport) vs what bypasses it (JSON-RPC, MCP)
- Built data flow compatibility matrix between cli-replay and gert capture formats (stdout, stderr, exit code, duration, timing all compatible; stdin and metadata differing)
- Evaluated concurrency considerations for parallel iterate and parallel steps
- Analyzed Scenario format gap between gert and cli-replay (exact argv match vs pattern matching)
- Documented gaps cli-replay fills: pattern matching, budget-based matching, unordered matching, shim interception, stdin matching
- Created RecordingExecutor wrapper pattern implementation sketch

## Decision Filed

**gene-gert-integration-points.md** — Proposes RecordingExecutor wrapper pattern as immediate implementation (50 lines of code, zero gert changes). Ranked three patterns: (1) RecordingExecutor wrapper, (2) `--mode record` first-class mode, (3) Scenario format bridge. Long-term: if cli-replay exports pkg/ API, gert could import cli-replay's matching engine.

## Key Findings

- Every CLI command in gert flows through ONE interface: `providers.CommandExecutor.Execute(ctx, command, args, env)`
- CommandResult already captures all necessary fields: stdout, stderr, exit code, duration
- Concurrency-safe: gert uses atomic file-level writes (JSONL append), existing cli-replay recording is thread-safe for parallel execution
- JSON-RPC and MCP transports completely bypass CommandExecutor (managed process lifecycle, not CLI execution)
- Format bridge (Pattern 3) straightforward due to structural similarity of gert's and cli-replay's scenario YAML

## Next Steps

Implement RecordingExecutor wrapper at injection point in `cmd/gert/main.go:runExec()`. Build scenario format converter for bidirectional replay. Propose Pattern 2 (`--mode record`) to gert upstream.
